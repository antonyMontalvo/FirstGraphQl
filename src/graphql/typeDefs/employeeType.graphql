# ./graphql/types/employeeType.graphql
type Employee {
  _id: String
  name: String
  position: String
  office: String
  salary: Int
}

type Query {
    employees: [Employee],
    employee(name: String): Employee #Busco por nombre ya que id en mongodb es distinto OBjectId()
}
